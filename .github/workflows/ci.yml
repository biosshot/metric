name: ci

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  foundation:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@1.88.0
        with:
          components: clippy,rustfmt
      - uses: Swatinem/rust-cache@v2
      - uses: actions/setup-go@v5
        with:
          go-version: 1.25.1
          cache-dependency-path: sdk-tests/go/go.sum
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: sdk-tests/python/requirements.lock.txt
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "25"
      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "9.0.x"
      - run: cargo fmt --all -- --check
      - run: cargo dep-graph --locked
      - run: python scripts/validate-compatibility.py
      - run: cargo clippy --workspace --all-targets --all-features --locked -- -D warnings
      - run: cargo test --workspace --all-targets --all-features --locked
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: |
            web/package-lock.json
            sdk-tests/browser/package-lock.json
            sdk-tests/node/package-lock.json
            sdk-tests/sentry-cli/package-lock.json
      - name: Verify Web
        working-directory: web
        run: |
          npm ci
          npm run format:check
          npm run lint
          npm test
          npm run build
      - name: Verify real Node SDK
        working-directory: sdk-tests/node
        run: |
          npm ci
          npm run format:check
          npm run lint
      - name: Verify real Browser SDK
        working-directory: sdk-tests/browser
        run: |
          npm ci
          npm run format:check
          npm run lint
          npm run build
          npx playwright install --with-deps chromium
      - name: Verify real Go SDK
        working-directory: sdk-tests/go
        run: |
          gofmt -d .
          go mod verify
          go test ./...
      - name: Verify real Python SDK
        run: python -m pip install -r sdk-tests/python/requirements.lock.txt
      - name: Verify real Java SDK
        run: node sdk-tests/java/prepare.mjs
      - name: Verify real .NET SDK
        run: |
          dotnet restore --locked-mode sdk-tests/dotnet/FaultkeepSdkCompatibility.csproj
          dotnet format --verify-no-changes --no-restore sdk-tests/dotnet/FaultkeepSdkCompatibility.csproj
          dotnet build --configuration Release --no-restore sdk-tests/dotnet/FaultkeepSdkCompatibility.csproj
      - name: Verify real Rust SDK
        run: cargo build --locked --manifest-path sdk-tests/rust/Cargo.toml
      - name: Run real SDK gates
        run: cargo test -p faultkeep-server --test sdk_compatibility_e2e -- --ignored --nocapture
      - name: Verify pinned Sentry CLI versions
        working-directory: sdk-tests/sentry-cli
        run: |
          npm ci
          npm run versions

  package:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: docker build --tag faultkeep:ci .
